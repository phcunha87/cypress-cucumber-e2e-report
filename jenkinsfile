pipeline{
    agent any
        stages {
            stage('Just Test') {
                steps {
                    bat 'npm run test'
                    bat 'npm run cy:report'
                }
            }
        }
}